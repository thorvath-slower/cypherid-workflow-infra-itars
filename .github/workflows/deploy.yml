name: CypherID Workflow Infra Deploy
run-name: Deploy CypherID Workflow Infra to ${{ inputs.environment }}

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'The environment to which to deploy'
        required: true
        type: string
        default: 'dev'

env:
  AWS_REGION : us-west-2
  GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
  DEPLOYMENT_ENVIRONMENT: ${{ inputs.environment }}

permissions:
  id-token: write
  contents: read

jobs:
  DeployTerraform:
    # CZID-244 (ITARS detach): hard off unless explicitly enabled. This assumes
    # arn:aws:iam::941377154785:role/gha-seqtoid and runs `make deploy` (full apply).
    # Set repo/environment variable ENABLE_CLOUD_CI=true only when wired to an owner account.
    if: ${{ vars.ENABLE_CLOUD_CI == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - name: Git clone the repository
        uses: actions/checkout@v4
      - name: configure aws credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::941377154785:role/gha-seqtoid
          role-session-name: GitHub_to_AWS_via_FederatedOIDC
          aws-region: ${{ env.AWS_REGION }}
      - name: Sts GetCallerIdentity
        run: |
          aws sts get-caller-identity
    #   - name: Install initial tools required by report_deployment_status.sh
    #     run: |
    #       sudo apt-get update
    #       sudo apt-get install jq
    #       sudo apt-get install -y httpie
    #   - name: Output GH Event Data
    #     run: |
    #       jq -r . "$GITHUB_EVENT_PATH"
      - name: Deploy dev
        run: |
          #trap 'scripts/report_deployment_status.sh failure' ERR
          #scripts/report_deployment_status.sh in_progress "Starting deployment"
          source scripts/init_ci_runner.sh
          make deploy